<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Unit Converter</title>
    <link rel="stylesheet" href="/static/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>

<body>
    <div class="container">
        <h1>Unit Converter</h1>
        <div class="tabs">
            {{#Converters}}
            <button {{#active}}id="defaultOpen" {{/active}} class="tablinks"
                onclick="openConverter(event, '{{Id}}')">{{Title}}</button>
            {{/Converters}}
        </div>

        {{#Converters}}
        <div id="{{Id}}" class="tabcontent">
            <form method="POST" action="{{Action}}">
                <label>Value</label>
                <input type="number" name="value" placeholder="Enter value" required>
                <label>From</label>
                <select name="from">
                    {{#Options}}
                    <option value="{{.}}">{{.}}</option>
                    {{/Options}}
                </select>
                <label>To</label>
                <select name="to">
                    {{#Options}}
                    <option value="{{.}}">{{.}}</option>
                    {{/Options}}
                </select>
                <button class="btn-submit" type="submit">
                    <i class="fas fa-exchange"></i> Convert
                </button>
            </form>

            {{#Result}}
            {{#Result.InputValue}}
            <div class="result">
                <h3>Result of the calculation</h3>
                <p>{{InputValue}} {{FromUnit}} = {{ConvertedValue}} {{ToUnit}}</p>
                <button class="btn-reset" onclick="resetForm()">
                    <i class="fas fa-refresh"></i> Reset
                </button>
            </div>
            {{/Result.InputValue}}
            {{^Result.InputValue}}{{/Result.InputValue}}
            {{/Result}}
        </div>
        {{/Converters}}
    </div>
</body>

<script src="/static/script.js"></script>

</html>